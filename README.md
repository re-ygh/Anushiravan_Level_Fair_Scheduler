حتماً 👍
این یه **نقشه راه قدم‌به‌قدم**ه که اگر دقیق جلو بری، **حتماً پروژه پاس میشه** و آخرش هم گیج نمی‌مونی. طوری نوشتم که بدون دانش کرنل هم بشه انجامش داد.

---

# 🧭 نقشه راه پیاده‌سازی پروژه ALFS

> 🎯 هدف: پیاده‌سازی **CFS ساده‌شده با Min-Heap** + ورودی UDS + خروجی scheduler

---

## 🧱 فاز ۰ — تصمیم‌های اولیه (۳۰ دقیقه)

**قبل از کدنویسی**

1. **زبان**:

   * پیشنهادی: **Python یا C++**
   * اگر فقط پاس شدن مهمه → Python کاملاً اوکیه
2. **ساختار برنامه**:

   * برنامه CLI که اجرا میشه مثل:

     ```bash
     ./alfs --cpus 4 --quantum 1 --socket ./socket.event
     ```

---

## 🧱 فاز ۱ — مدل داده‌ها (خیلی مهم)

**اول اینا رو تعریف کن، بعد برو سراغ منطق**

### 1️⃣ Task (Process)

برای هر task این فیلدها رو نگه دار:

```text
id
state: RUNNABLE | BLOCKED | EXITED
nice
weight
vruntime
cpu_affinity (set)
cgroup_id
current_cpu (یا None)
```

📌 `weight` رو از جدول nice لینوکس بگیر

---

### 2️⃣ CPU

```text
cpu_id
current_task (یا idle)
```

---

### 3️⃣ Runqueue (Heap)

* یک **min-heap** از taskهای RUNNABLE
* کلید heap = `vruntime`

---

### 4️⃣ Cgroup (نسخه ساده)

```text
id
cpuShares
cpuMask
tasks (set)
```

فعلاً quota رو بی‌خیال شو

---

## 🧱 فاز ۲ — زیرساخت ارتباطی (UDS)

**بدون scheduler هنوز**

1. ساخت **Unix Domain Socket**
2. گوش دادن برای پیام
3. دریافت JSON
4. parse کردن JSON به eventها

📌 تست کن فقط اینو:

```text
socket → json → print
```

---

## 🧱 فاز ۳ — مدیریت eventها (مغز پروژه)

### ترتیب درست کار:

در هر `vtime`:

```
receive events
apply events
schedule CPUs
output result
```

---

### 3️⃣ پیاده‌سازی eventها (به این ترتیب)

#### ✅ TASK_CREATE

```text
- ساخت Task
- vruntime = min_vruntime فعلی
- state = RUNNABLE
- push به heap
```

---

#### ✅ TASK_BLOCK

```text
- state = BLOCKED
- از heap حذف شود
- از CPU آزاد شود
```

---

#### ✅ TASK_UNBLOCK

```text
- state = RUNNABLE
- vruntime = max(current_vruntime, min_vruntime)
- push به heap
```

---

#### ✅ TASK_EXIT

```text
- حذف کامل از همه‌جا
```

---

#### ✅ TASK_YIELD

```text
- vruntime = max_vruntime
- push مجدد به heap
```

---

#### ✅ TASK_SETNICE

```text
- update nice
- update weight
```

---

#### ✅ TASK_SET_AFFINITY

```text
- cpu_affinity = جدید
```

---

## 🧱 فاز ۴ — الگوریتم Scheduler (CFS ساده)

برای هر CPU:

1. از heap کوچک‌ترین vruntime رو بردار
2. اگر affinity سازگار نیست → موقتاً رد کن
3. اگر پیدا نشد → `idle`
4. task انتخاب‌شده:

   * vruntime += delta
   * delta = `quantum * (1024 / weight)`

📌 بعدش task رو دوباره بذار تو heap

---

## 🧱 فاز ۵ — چند CPU و Preemption

(نسخه ساده ولی قابل قبول)

* هر tick:

  * همه CPUها از صفر schedule میشن
* اگر task قبلی ≠ task جدید → preemption++

---

## 🧱 فاز ۶ — تولید خروجی

حتماً این فرمت:

```json
{
  "vtime": 10,
  "schedule": ["T1", "idle", "T2"]
}
```

بعداً می‌تونی meta اضافه کنی (اختیاری)

---

## 🧱 فاز ۷ — تست دستی

سناریو ساده:

1. CPU = 1
2. دو task
3. یکی block/unblock
4. خروجی رو چشمی بررسی کن

---

## 🧱 فاز ۸ — تمیزکاری و توضیح‌پذیری

برای نمره:

* بتونی جواب بدی:

  * vruntime چیه؟
  * چرا heap؟
  * yield چه فرقی با block داره؟
  * nice چطور اثر می‌ذاره؟

---

# ✅ حداقل چیزی که اگر بسازی «پاس میشی»

✔ UDS
✔ JSON events
✔ heap
✔ vruntime + nice
✔ schedule خروجی

---

## 🧠 پیشنهاد هوشمندانه

اگر زمانت کمه:

* ❌ quota
* ❌ LITTLE.Big
* ❌ metadata
* ❌ تحقیق

همه رو حذف کن → پاس میشی

---

## ➕ قدم بعدی؟

می‌تونم:

1. 📄 یه **چک‌لیست نهایی قبل تحویل**
2. 🧪 یه **سناریوی تست آماده**
3. 🧩 pseudo-code کل پروژه
4. 🧱 اسکلت کد (Python یا C++)

بگو کدومو می‌خوای 👌




























عالی، حالا می‌ریم سراغ **ساختار حرفه‌ای پروژه در C++**؛ طوری که:

* تمیز باشه
* قابل توضیح به استاد
* بدون over-engineering
* و دقیقاً مناسب همین پروژه Scheduler

---

# 📁 ساختار پیشنهادی پروژه ALFS (C++)

```
alfs/
│
├── CMakeLists.txt
│
├── include/
│   ├── scheduler/
│   │   ├── Scheduler.hpp
│   │   ├── RunQueue.hpp
│   │   ├── Task.hpp
│   │   ├── CPU.hpp
│   │   ├── Cgroup.hpp
│   │   └── Event.hpp
│   │
│   └── utils/
│       ├── Json.hpp
│       ├── Socket.hpp
│       └── Config.hpp
│
├── src/
│   ├── main.cpp
│   │
│   ├── scheduler/
│   │   ├── Scheduler.cpp
│   │   ├── RunQueue.cpp
│   │   ├── Task.cpp
│   │   ├── CPU.cpp
│   │   └── Cgroup.cpp
│   │
│   └── utils/
│       ├── Json.cpp
│       ├── Socket.cpp
│       └── Config.cpp
│
└── README.md
```

---

## 🧠 منطق این ساختار چیه؟

### 1️⃣ `Task`

نماینده‌ی یک process/thread در سیستم

**Task.hpp**

```cpp
struct Task {
    int id;
    int nice;
    double vruntime;
    double weight;
    bool runnable;
    std::set<int> affinity;
    int cgroup_id;
};
```

📌 این همون چیزیه که استاد انتظار داره وقتی بگه:

> «هر task چه اطلاعاتی داره؟»

---

### 2️⃣ `RunQueue`

صف آماده اجرا (Min-Heap)

**RunQueue.hpp**

```cpp
class RunQueue {
public:
    void push(Task*);
    Task* pop();
    void remove(Task*);
    bool empty() const;
};
```

📌 داخلش از `std::priority_queue` با comparator معکوس استفاده می‌کنی.

---

### 3️⃣ `CPU`

شبیه‌سازی یک CPU منطقی

```cpp
struct CPU {
    int id;
    Task* current;
};
```

---

### 4️⃣ `Scheduler`

🧠 مغز پروژه

**Scheduler.hpp**

```cpp
class Scheduler {
public:
    Scheduler(int cpu_count, int quantum);
    void handle_event(const Event&);
    void tick(int vtime);
};
```

داخل `tick()`:

1. eventها اعمال می‌شن
2. برای هر CPU task انتخاب می‌شه
3. vruntime آپدیت می‌شه
4. خروجی چاپ می‌شه

---

### 5️⃣ `Event`

برای JSON ورودی

```cpp
enum class EventType {
    TASK_CREATE,
    TASK_BLOCK,
    TASK_UNBLOCK,
    TASK_EXIT,
    TASK_YIELD,
    TASK_SETNICE,
    TASK_SET_AFFINITY
};

struct Event {
    EventType type;
    int task_id;
    int value;
};
```

---

### 6️⃣ `Socket`

ارتباط با تست‌کننده (UDS)

```cpp
class UnixSocket {
public:
    void connect(const std::string& path);
    std::string receive();
};
```

📌 تمام `sys/socket.h` اینجاست، main قاطی نمی‌شه.

---

### 7️⃣ `Json`

Parse ورودی / تولید خروجی

* ورودی: `string → Event`
* خروجی: `schedule → JSON`

می‌تونی از `nlohmann/json` استفاده کنی.

---

## 🚀 جریان اجرای برنامه (`main.cpp`)

```cpp
int main() {
    Config cfg;
    Scheduler scheduler(cfg.cpus, cfg.quantum);
    UnixSocket socket;

    socket.connect(cfg.socket_path);

    while (true) {
        auto json = socket.receive();
        auto events = parse_events(json);
        scheduler.tick(events.vtime);
    }
}
```

---

## ✅ چرا این ساختار عالیه؟

✔ تفکیک مسئولیت‌ها
✔ قابل توضیح تو دفاع
✔ هر بخش جدا تست می‌شه
✔ اگه خواستی امتیازی بزنی، جا داره

---

## 🎯 حداقل فایل‌هایی که **واقعاً باید بنویسی**

اگر کم‌وقت بودی:

* `Task`
* `RunQueue`
* `Scheduler`
* `Socket`
* `main`

بقیه رو می‌تونی ساده یا حتی ادغام کنی.

---

## ➕ قدم بعدی منطقی

به‌ترتیب پیشنهاد می‌کنم:

1. `Task + RunQueue`
2. `Scheduler::tick()`
3. Socket ساده
4. JSON

اگر بخوای، قدم بعدی می‌تونم:

* 🧩 **اسکلت واقعی C++ برای این ساختار**
* 🧪 **RunQueue با comparator درست**
* 🔌 **کد کامل UDS در لینوکس**
* 🧠 **پیاده‌سازی دقیق vruntime**

بگو از کدوم شروع کنیم 👌
